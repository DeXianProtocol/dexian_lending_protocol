CALL_METHOD
    Address("${account}")
    "lock_fee"
    Decimal("10")
;
CALL_METHOD
    Address("${def_interest_model}")
    "get_interest_rate"
    Decimal("${borrow_ratio}")
    Decimal("${stable_ratio}")
    Enum<${enum}u8>()
;
