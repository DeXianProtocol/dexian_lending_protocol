CALL_METHOD
    Address("${account}")
    "lock_fee"
    Decimal("1000")
;
CALL_METHOD
  Address("${account}")
  "withdraw"
  Address("${repay_token}")
  Decimal("${amount}")
;
TAKE_FROM_WORKTOP
  Address("${repay_token}")
  Decimal("${amount}")
  Bucket("bucket1")
;
CALL_METHOD
    Address("${lending_component}")
    "repay"
    Bucket("bucket1")
    ${cdp_id}u64
;
CALL_METHOD
    Address("${account}")
    "deposit_batch"
    Expression("ENTIRE_WORKTOP")
;
